package com.virinchi.feature.composite.grandround.adapterCheck

import androidx.recyclerview.widget.RecyclerView
import com.example.experiments.data.TestData
import com.example.experiments.databinding.GrWebinarItemLayoutBinding

class GRWebinarViewHolder(private val binding: GrWebinarItemLayoutBinding) :
    RecyclerView.ViewHolder(binding.root) {

    fun bind(model: TestData) {

    }
}